import 'package:flutter/material.dart';

class Constructors extends StatelessWidget {
  const Constructors({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Constructors'),
      ),
      body: ListView(
        children: [],
      ),
    );
  }
}
